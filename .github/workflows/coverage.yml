name: Coverage

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v6

      - name: Cache Maven repository
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21
          cache: maven

      - name: Build
        run: |
          .\install_certs.bat
          .\build.bat

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: >-
            org.sf.feeling.decompiler.cfr.tests/target/jacoco-report/jacoco.xml,
            org.sf.feeling.decompiler.jd.tests/target/jacoco-report/jacoco.xml,
            org.sf.feeling.decompiler.procyon.tests/target/jacoco-report/jacoco.xml,
            org.sf.feeling.decompiler.source.attach.tests/target/jacoco-report/jacoco.xml,
            org.sf.feeling.decompiler.tests/target/jacoco-report/jacoco.xml,
            org.sf.feeling.decompiler.vineflower.tests/target/jacoco-report/jacoco.xml
          verbose: true
          fail_ci_if_error: true
